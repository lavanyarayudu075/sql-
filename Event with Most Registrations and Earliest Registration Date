SELECT event_name,
 COUNT(*) AS total_registrations, 
 MIN(registration_date) AS earliest_date
FROM event_registrations
GROUP BY event_name
ORDER BY total_registrations DESC, earliest_date ASC
LIMIT 1;
